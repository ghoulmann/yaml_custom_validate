{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "pattern": "^[A-Z]{2}\\d{3}$"
      },
      "description": {
        "type": "string",
        "titlecase": "true"
      },
      "location": {
        "type": "string",
        "pattern": "^[a-z_]+$"
      }
    },
    "required": ["code", "description", "location"],
    "propertyOrder": ["code", "description", "location"],
    "additionalProperties": true,
    "minProperties": 3
  }
}
